#foreach ($animal in $animals)
  #if (1 == $animal.getPersonid())
    <ul>
      <li>Name: $animal.getName()</li>
      <li>Gender: $animal.getGender()</li>
      <li>date of admittance: $animal.getDta()</li>
      <li>Breed: $animal.getBreed()</li>
      <li>Type: $animal.getType()</li>
    </ul>
  #end
#end
<br>
<a href="/animals/new">Add a new animal</a>
<br>
<a href="/">Back to the home page</a>
